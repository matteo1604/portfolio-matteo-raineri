Dammi un rapporto veloce sullo stato del progetto:

1. Leggi il CLAUDE.md e mostrami l'ultimo session log
2. Esegui `git status` e `git log --oneline -5` per vedere lo stato attuale
3. Controlla se ci sono errori TypeScript con `npx tsc --noEmit 2>&1 | head -10`
4. Basandoti sulla priority order nel CLAUDE.md, suggerisci su cosa lavorare nella prossima sessione

Rispondi in modo conciso, max 15 righe.